import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  screenContainer: {
    flex: 1,
    flexDirection: "row",
    backgroundColor: "black",
  },
  baseText: {
    color: "white",
  },
});
